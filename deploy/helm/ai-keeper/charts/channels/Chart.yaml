# Sub-chart: channels
# Channel adapters (feishu first, more in P1)
#
# Skeleton populated by task 1.3. Real templates are added by the task
# that owns this component (see tasks.md mapping).
apiVersion: v2
name: channels
description: Channel adapters (feishu first, more in P1)
type: application
version: 0.0.1
appVersion: "0.0.1"
kubeVersion: ">=1.28.0-0"
keywords:
  - aip
  - channels
maintainers:
  - name: The AIP Authors
