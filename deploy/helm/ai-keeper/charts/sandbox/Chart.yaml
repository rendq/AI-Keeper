# Sub-chart: sandbox
# Sandbox RuntimeClasses — gVisor, Firecracker, Kata
#
# Provides RuntimeClass resources for secure agent execution sandboxes.
# Cluster detection logic: Agent Controller checks RuntimeClass existence during reconcile.
apiVersion: v2
name: sandbox
description: Sandbox RuntimeClasses — gVisor, Firecracker, Kata containers
type: application
version: 0.0.1
appVersion: "0.0.1"
kubeVersion: ">=1.28.0-0"
keywords:
  - aip
  - sandbox
  - gvisor
  - firecracker
  - kata
maintainers:
  - name: The AIP Authors
