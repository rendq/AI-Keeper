# Sub-chart: router
# Model Router — CEL rules, weighted routing, fallback chains
#
# Skeleton populated by task 1.3. Real templates are added by the task
# that owns this component (see tasks.md mapping).
apiVersion: v2
name: router
description: Model Router — CEL rules, weighted routing, fallback chains
type: application
version: 0.0.1
appVersion: "0.0.1"
kubeVersion: ">=1.28.0-0"
keywords:
  - aip
  - router
maintainers:
  - name: The AIP Authors
