# Sub-chart: manager
# AIP controller manager (CRD reconcilers + admission webhook)
#
# Skeleton populated by task 1.3. Real templates are added by the task
# that owns this component (see tasks.md mapping).
apiVersion: v2
name: manager
description: AIP controller manager (CRD reconcilers + admission webhook)
type: application
version: 0.0.1
appVersion: "0.0.1"
kubeVersion: ">=1.28.0-0"
keywords:
  - aip
  - manager
maintainers:
  - name: The AIP Authors
