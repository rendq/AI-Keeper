# Sub-chart: runtime
# Agent / Skill Runtime — Knative + Python aik_runtime
#
# Skeleton populated by task 1.3. Real templates are added by the task
# that owns this component (see tasks.md mapping).
apiVersion: v2
name: runtime
description: Agent / Skill Runtime — Knative + Python aik_runtime
type: application
version: 0.0.1
appVersion: "0.0.1"
kubeVersion: ">=1.28.0-0"
keywords:
  - aip
  - runtime
maintainers:
  - name: The AIP Authors
