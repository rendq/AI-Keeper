# Sub-chart: pdp
# Policy Decision Point — OPA-backed Decide() gRPC service
#
# Skeleton populated by task 1.3. Real templates are added by the task
# that owns this component (see tasks.md mapping).
apiVersion: v2
name: pdp
description: Policy Decision Point — OPA-backed Decide() gRPC service
type: application
version: 0.0.1
appVersion: "0.0.1"
kubeVersion: ">=1.28.0-0"
keywords:
  - aip
  - pdp
maintainers:
  - name: The AIP Authors
