# Sub-chart: gateway
# AIP Gateway (Envoy + Go xDS) — channel ingress + auth + rate-limit
#
# Skeleton populated by task 1.3. Real templates are added by the task
# that owns this component (see tasks.md mapping).
apiVersion: v2
name: gateway
description: AIP Gateway (Envoy + Go xDS) — channel ingress + auth + rate-limit
type: application
version: 0.0.1
appVersion: "0.0.1"
kubeVersion: ">=1.28.0-0"
keywords:
  - aip
  - gateway
maintainers:
  - name: The AIP Authors
