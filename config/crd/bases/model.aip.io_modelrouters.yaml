---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: modelrouters.model.ai-keeper.io
spec:
  group: model.ai-keeper.io
  names:
    categories:
    - aip
    kind: ModelRouter
    listKind: ModelRouterList
    plural: modelrouters
    shortNames:
    - mr
    singular: modelrouter
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.alias
      name: Alias
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          ModelRouter routes a logical model alias to one or more concrete
          ModelEndpoints.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ModelRouterSpec is the desired state.
            properties:
              alias:
                description: Alias the upstream uses (e.g. "reasoner", "embedder").
                maxLength: 64
                minLength: 1
                type: string
              cache:
                description: ModelRouterCache configures semantic caching.
                properties:
                  enabled:
                    type: boolean
                  mode:
                    enum:
                    - exact
                    - semantic
                    - hybrid
                    type: string
                  ref:
                    description: |-
                      ResourceRef is the canonical AIP URI used to reference any other resource
                      (skills, tools, models, data, prompts, channels, connectors, memory,
                      quota, ref, siem, policy). The accepted shape is
                      `<scheme>://<path>[@<version>]`.

                      Validates: Requirements A2.1 (regex enforcement), F25 (Parse∘Format
                      round-trip).
                    maxLength: 512
                    minLength: 8
                    pattern: ^(skill|agent|tool|model|data|prompt|channel|connector|memory|quota|ref|siem|policy)://[A-Za-z0-9._/\-]+(@[A-Za-z0-9._\-+]+)?$
                    type: string
                  similarityThreshold:
                    maximum: 1
                    minimum: 0
                    type: number
                  ttl:
                    description: |-
                      Duration is a coarse-grained, human-friendly duration string used in
                      CRDs. Supported units: ns, us, ms, s, m, h, d, w.

                      Validates: Requirements A2.2.
                    maxLength: 32
                    minLength: 2
                    pattern: ^\d+(ns|us|ms|s|m|h|d|w)$
                    type: string
                type: object
              defaultEndpoint:
                description: |-
                  ResourceRef is the canonical AIP URI used to reference any other resource
                  (skills, tools, models, data, prompts, channels, connectors, memory,
                  quota, ref, siem, policy). The accepted shape is
                  `<scheme>://<path>[@<version>]`.

                  Validates: Requirements A2.1 (regex enforcement), F25 (Parse∘Format
                  round-trip).
                maxLength: 512
                minLength: 8
                pattern: ^(skill|agent|tool|model|data|prompt|channel|connector|memory|quota|ref|siem|policy)://[A-Za-z0-9._/\-]+(@[A-Za-z0-9._\-+]+)?$
                type: string
              loadBalancing:
                description: LoadBalancing strategy.
                enum:
                - round_robin
                - least_latency
                - least_cost
                - weighted
                - sticky_session
                type: string
              rules:
                items:
                  description: ModelRouterRule is one routing rule.
                  properties:
                    endpoint:
                      description: |-
                        ResourceRef is the canonical AIP URI used to reference any other resource
                        (skills, tools, models, data, prompts, channels, connectors, memory,
                        quota, ref, siem, policy). The accepted shape is
                        `<scheme>://<path>[@<version>]`.

                        Validates: Requirements A2.1 (regex enforcement), F25 (Parse∘Format
                        round-trip).
                      maxLength: 512
                      minLength: 8
                      pattern: ^(skill|agent|tool|model|data|prompt|channel|connector|memory|quota|ref|siem|policy)://[A-Za-z0-9._/\-]+(@[A-Za-z0-9._\-+]+)?$
                      type: string
                    weight:
                      format: int32
                      maximum: 100
                      minimum: 0
                      type: integer
                    when:
                      description: ModelRouterRuleWhen captures rule predicates.
                      properties:
                        compliance:
                          items:
                            type: string
                          type: array
                        contextLengthMin:
                          format: int32
                          minimum: 0
                          type: integer
                        costSensitive:
                          type: boolean
                        expression:
                          description: Expression is a CEL predicate.
                          maxLength: 4096
                          type: string
                        latencySensitive:
                          type: boolean
                        taskType:
                          enum:
                          - chat
                          - classify
                          - extract
                          - summarize
                          - code
                          - math
                          - vision
                          - embedding
                          type: string
                        tenant:
                          maxLength: 253
                          type: string
                      type: object
                  required:
                  - endpoint
                  type: object
                minItems: 1
                type: array
            required:
            - alias
            - rules
            type: object
          status:
            description: ModelRouterStatus is the observed state.
            properties:
              cacheHitRate:
                type: number
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              distribution:
                items:
                  description: ModelRouterDistribution is per-endpoint routing distribution.
                  properties:
                    endpoint:
                      description: |-
                        ResourceRef is the canonical AIP URI used to reference any other resource
                        (skills, tools, models, data, prompts, channels, connectors, memory,
                        quota, ref, siem, policy). The accepted shape is
                        `<scheme>://<path>[@<version>]`.

                        Validates: Requirements A2.1 (regex enforcement), F25 (Parse∘Format
                        round-trip).
                      maxLength: 512
                      minLength: 8
                      pattern: ^(skill|agent|tool|model|data|prompt|channel|connector|memory|quota|ref|siem|policy)://[A-Za-z0-9._/\-]+(@[A-Za-z0-9._\-+]+)?$
                      type: string
                    requests24h:
                      format: int64
                      type: integer
                    weight:
                      format: int32
                      type: integer
                  required:
                  - endpoint
                  type: object
                type: array
              observedGeneration:
                format: int64
                type: integer
              phase:
                description: |-
                  Phase is the coarse-grained status.phase value reported by every AIP
                  controller. The combined enum covers every controller in the platform
                  (Skill / Agent / Policy / Tool / DataSource / KB / ModelEndpoint /
                  ModelRouter / Tenant / SA / Budget / Quota), so a single Phase type can
                  be embedded in any status struct.

                  Validates: Requirements A3, A4, A5 (state machines reference these
                  values).
                enum:
                - Pending
                - Validating
                - Resolving
                - Building
                - Registering
                - Evaluating
                - Active
                - Degraded
                - Failed
                - Deprecated
                - Terminating
                - Suspended
                - Expired
                - ResolvingSkills
                - AttachingPolicies
                - Provisioning
                - Deploying
                - Configuring
                - RollingOut
                - Running
                - Paused
                - RolledBack
                type: string
              requestsRouted24h:
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
