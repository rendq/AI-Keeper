---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: skills.skill.ai-keeper.io
spec:
  group: skill.ai-keeper.io
  names:
    categories:
    - aip
    kind: Skill
    listKind: SkillList
    plural: skills
    shortNames:
    - sk
    singular: skill
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.version
      name: Version
      type: string
    - jsonPath: .spec.stability
      name: Stage
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Skill is the platform's unit of reusable business capability.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SkillSpec is the desired state of a Skill.
            properties:
              cost:
                description: Cost block (per-call estimator + budget).
                properties:
                  budget:
                    description: Budget per call.
                    properties:
                      tokensPerCall:
                        format: int64
                        minimum: 0
                        type: integer
                      usdPerCall:
                        description: |-
                          MoneyAmount is the canonical wire representation of a USD amount used
                          in CostBlock fields. We store money as a string with a constrained
                          pattern (rather than `float64`) to keep CRD diffs bit-for-bit
                          reproducible and to avoid the JSON-number rounding that breaks audit
                          hashing (Requirements F4, F22).
                        maxLength: 32
                        pattern: ^\d+(\.\d+)?$
                        type: string
                    type: object
                  estimator:
                    description: Estimator config.
                    properties:
                      historicalWindow:
                        description: HistoricalWindow used by historical estimators.
                        maxLength: 32
                        minLength: 2
                        pattern: ^\d+(ns|us|ms|s|m|h|d|w)$
                        type: string
                      tokensPerCall:
                        description: TokensPerCall percentiles.
                        properties:
                          p50:
                            description: |-
                              MoneyAmount is the canonical wire representation of a USD amount used
                              in CostBlock fields. We store money as a string with a constrained
                              pattern (rather than `float64`) to keep CRD diffs bit-for-bit
                              reproducible and to avoid the JSON-number rounding that breaks audit
                              hashing (Requirements F4, F22).
                            maxLength: 32
                            pattern: ^\d+(\.\d+)?$
                            type: string
                          p95:
                            description: |-
                              MoneyAmount is the canonical wire representation of a USD amount used
                              in CostBlock fields. We store money as a string with a constrained
                              pattern (rather than `float64`) to keep CRD diffs bit-for-bit
                              reproducible and to avoid the JSON-number rounding that breaks audit
                              hashing (Requirements F4, F22).
                            maxLength: 32
                            pattern: ^\d+(\.\d+)?$
                            type: string
                          p99:
                            description: |-
                              MoneyAmount is the canonical wire representation of a USD amount used
                              in CostBlock fields. We store money as a string with a constrained
                              pattern (rather than `float64`) to keep CRD diffs bit-for-bit
                              reproducible and to avoid the JSON-number rounding that breaks audit
                              hashing (Requirements F4, F22).
                            maxLength: 32
                            pattern: ^\d+(\.\d+)?$
                            type: string
                        type: object
                      type:
                        description: Type of estimator.
                        enum:
                        - static
                        - model_based
                        - historical
                        type: string
                      usdPerCall:
                        description: UsdPerCall percentiles.
                        properties:
                          p50:
                            description: |-
                              MoneyAmount is the canonical wire representation of a USD amount used
                              in CostBlock fields. We store money as a string with a constrained
                              pattern (rather than `float64`) to keep CRD diffs bit-for-bit
                              reproducible and to avoid the JSON-number rounding that breaks audit
                              hashing (Requirements F4, F22).
                            maxLength: 32
                            pattern: ^\d+(\.\d+)?$
                            type: string
                          p95:
                            description: |-
                              MoneyAmount is the canonical wire representation of a USD amount used
                              in CostBlock fields. We store money as a string with a constrained
                              pattern (rather than `float64`) to keep CRD diffs bit-for-bit
                              reproducible and to avoid the JSON-number rounding that breaks audit
                              hashing (Requirements F4, F22).
                            maxLength: 32
                            pattern: ^\d+(\.\d+)?$
                            type: string
                          p99:
                            description: |-
                              MoneyAmount is the canonical wire representation of a USD amount used
                              in CostBlock fields. We store money as a string with a constrained
                              pattern (rather than `float64`) to keep CRD diffs bit-for-bit
                              reproducible and to avoid the JSON-number rounding that breaks audit
                              hashing (Requirements F4, F22).
                            maxLength: 32
                            pattern: ^\d+(\.\d+)?$
                            type: string
                        type: object
                    type: object
                type: object
              evaluation:
                description: Evaluation suite for stage gating.
                properties:
                  evalSet:
                    description: EvalSet ref.
                    maxLength: 512
                    minLength: 8
                    pattern: ^(skill|agent|tool|model|data|prompt|channel|connector|memory|quota|ref|siem|policy)://[A-Za-z0-9._/\-]+(@[A-Za-z0-9._\-+]+)?$
                    type: string
                  gates:
                    additionalProperties:
                      additionalProperties:
                        type: string
                      type: object
                    description: |-
                      Gates for stage promotion. Outer key = target stage, inner map =
                      metric → CEL expression.
                    type: object
                  redTeamSet:
                    description: RedTeamSet ref.
                    maxLength: 512
                    minLength: 8
                    pattern: ^(skill|agent|tool|model|data|prompt|channel|connector|memory|quota|ref|siem|policy)://[A-Za-z0-9._/\-]+(@[A-Za-z0-9._\-+]+)?$
                    type: string
                  schedule:
                    description: Schedule is a cron string controlling Eval_Runner
                      cadence.
                    maxLength: 128
                    type: string
                type: object
              governance:
                description: |-
                  Governance block (classification, residency, PII, compliance,
                  retention).
                properties:
                  classification:
                    description: Classification applied to data flowing through the
                      resource.
                    enum:
                    - public
                    - internal
                    - confidential
                    - restricted
                    - secret
                    type: string
                  compliance:
                    description: Compliance regimes required for this resource.
                    properties:
                      reportTemplate:
                        description: ReportTemplate references a compliance reporting
                          template.
                        maxLength: 512
                        minLength: 8
                        pattern: ^(skill|agent|tool|model|data|prompt|channel|connector|memory|quota|ref|siem|policy)://[A-Za-z0-9._/\-]+(@[A-Za-z0-9._\-+]+)?$
                        type: string
                      required:
                        description: Required compliance tags (e.g. "GDPR", "HIPAA",
                          "等保三级").
                        items:
                          type: string
                        type: array
                    type: object
                  dataResidency:
                    description: DataResidency controls geographic placement of processing.
                    properties:
                      allowedRegions:
                        description: |-
                          AllowedRegions lists the regions in which a request may be
                          processed (e.g. "cn-north", "eu-west").
                        items:
                          type: string
                        type: array
                      crossBorder:
                        description: |-
                          CrossBorder controls whether transfers across the residency
                          boundary are permitted.
                        enum:
                        - forbidden
                        - allowed_with_approval
                        - allowed
                        type: string
                    type: object
                  holds:
                    description: |-
                      Holds carries legal-hold tags. Audit events inheriting these tags
                      are exempt from automatic deletion until the holds are cleared.
                    items:
                      type: string
                    type: array
                  pii:
                    description: PII policy.
                    properties:
                      onInput:
                        description: OnInput controls inbound DLP behaviour.
                        enum:
                        - ignore
                        - detect
                        - detect_and_mask
                        - detect_and_block
                        type: string
                      onOutput:
                        description: OnOutput controls outbound DLP behaviour.
                        enum:
                        - ignore
                        - detect
                        - detect_and_mask
                        - detect_and_block
                        type: string
                      patternsRef:
                        description: |-
                          PatternsRef points at a `ref://patterns/<name>` resource that holds
                          the regex / dictionary patterns the DLP engine uses.
                        maxLength: 512
                        minLength: 8
                        pattern: ^(skill|agent|tool|model|data|prompt|channel|connector|memory|quota|ref|siem|policy)://[A-Za-z0-9._/\-]+(@[A-Za-z0-9._\-+]+)?$
                        type: string
                    type: object
                  retentionDays:
                    description: |-
                      RetentionDays is how many days raw payloads may be retained
                      (audit-related; mirrors `retention` Duration on Skill governance
                      but is expressed in days for budget UIs).
                    format: int32
                    maximum: 36500
                    minimum: 0
                    type: integer
                type: object
              implementation:
                description: Implementation runtime.
                properties:
                  promptTemplate:
                    description: PromptTemplate to apply.
                    properties:
                      inline:
                        description: Inline template text.
                        maxLength: 65536
                        type: string
                      ref:
                        description: Ref to a prompt://... resource.
                        maxLength: 512
                        minLength: 8
                        pattern: ^(skill|agent|tool|model|data|prompt|channel|connector|memory|quota|ref|siem|policy)://[A-Za-z0-9._/\-]+(@[A-Za-z0-9._\-+]+)?$
                        type: string
                    type: object
                  requires:
                    description: Requires lists external resources this Skill depends
                      on.
                    properties:
                      dataSources:
                        items:
                          description: SkillDataSourceDep depends on a DataSource
                            (or KnowledgeBase).
                          properties:
                            ref:
                              description: |-
                                ResourceRef is the canonical AIP URI used to reference any other resource
                                (skills, tools, models, data, prompts, channels, connectors, memory,
                                quota, ref, siem, policy). The accepted shape is
                                `<scheme>://<path>[@<version>]`.

                                Validates: Requirements A2.1 (regex enforcement), F25 (Parse∘Format
                                round-trip).
                              maxLength: 512
                              minLength: 8
                              pattern: ^(skill|agent|tool|model|data|prompt|channel|connector|memory|quota|ref|siem|policy)://[A-Za-z0-9._/\-]+(@[A-Za-z0-9._\-+]+)?$
                              type: string
                          required:
                          - ref
                          type: object
                        type: array
                      models:
                        items:
                          description: SkillModelDep is a dependency on a model alias.
                          properties:
                            alias:
                              description: Alias used by the runtime (e.g. reasoner,
                                embedder).
                              maxLength: 64
                              minLength: 1
                              type: string
                            fallback:
                              description: Fallback chain used when the primary endpoint
                                fails.
                              items:
                                description: |-
                                  ResourceRef is the canonical AIP URI used to reference any other resource
                                  (skills, tools, models, data, prompts, channels, connectors, memory,
                                  quota, ref, siem, policy). The accepted shape is
                                  `<scheme>://<path>[@<version>]`.

                                  Validates: Requirements A2.1 (regex enforcement), F25 (Parse∘Format
                                  round-trip).
                                maxLength: 512
                                minLength: 8
                                pattern: ^(skill|agent|tool|model|data|prompt|channel|connector|memory|quota|ref|siem|policy)://[A-Za-z0-9._/\-]+(@[A-Za-z0-9._\-+]+)?$
                                type: string
                              type: array
                            purpose:
                              description: Purpose is the reason this dependency exists.
                              enum:
                              - reasoning
                              - embedding
                              - vision
                              - code
                              - classification
                              - rerank
                              type: string
                            ref:
                              description: Ref points at the ModelEndpoint or ModelRouter.
                              maxLength: 512
                              minLength: 8
                              pattern: ^(skill|agent|tool|model|data|prompt|channel|connector|memory|quota|ref|siem|policy)://[A-Za-z0-9._/\-]+(@[A-Za-z0-9._\-+]+)?$
                              type: string
                          required:
                          - alias
                          - ref
                          type: object
                        type: array
                      skills:
                        items:
                          description: |-
                            SkillSubSkillDep depends on another Skill with an optional version
                            constraint string (npm-style range, validated by the resolver, not at
                            admission time).
                          properties:
                            ref:
                              description: |-
                                ResourceRef is the canonical AIP URI used to reference any other resource
                                (skills, tools, models, data, prompts, channels, connectors, memory,
                                quota, ref, siem, policy). The accepted shape is
                                `<scheme>://<path>[@<version>]`.

                                Validates: Requirements A2.1 (regex enforcement), F25 (Parse∘Format
                                round-trip).
                              maxLength: 512
                              minLength: 8
                              pattern: ^(skill|agent|tool|model|data|prompt|channel|connector|memory|quota|ref|siem|policy)://[A-Za-z0-9._/\-]+(@[A-Za-z0-9._\-+]+)?$
                              type: string
                            versionConstraint:
                              description: VersionConstraint is an npm-style semver
                                range.
                              maxLength: 128
                              type: string
                          required:
                          - ref
                          type: object
                        type: array
                      tools:
                        items:
                          description: SkillToolDep depends on a Tool resource.
                          properties:
                            ref:
                              description: |-
                                ResourceRef is the canonical AIP URI used to reference any other resource
                                (skills, tools, models, data, prompts, channels, connectors, memory,
                                quota, ref, siem, policy). The accepted shape is
                                `<scheme>://<path>[@<version>]`.

                                Validates: Requirements A2.1 (regex enforcement), F25 (Parse∘Format
                                round-trip).
                              maxLength: 512
                              minLength: 8
                              pattern: ^(skill|agent|tool|model|data|prompt|channel|connector|memory|quota|ref|siem|policy)://[A-Za-z0-9._/\-]+(@[A-Za-z0-9._\-+]+)?$
                              type: string
                          required:
                          - ref
                          type: object
                        type: array
                    type: object
                  runtime:
                    description: Runtime parameters.
                    properties:
                      engine:
                        description: |-
                          Engine identifies the runtime engine (e.g. aip-runtime/v2,
                          langgraph, temporal, custom).
                        type: string
                      entrypoint:
                        description: Entrypoint is the function symbol to call.
                        type: string
                      image:
                        description: Image is the container image to run.
                        type: string
                    type: object
                  type:
                    description: Type of implementation.
                    enum:
                    - function
                    - workflow
                    - agentic
                    - mcp_tool
                    - external_api
                    type: string
                required:
                - type
                type: object
              interface:
                description: Interface contract.
                properties:
                  examples:
                    description: Examples for the developer-facing UI.
                    items:
                      description: |-
                        SkillExample is an input/output example pair attached to the Skill
                        interface. Inputs and outputs are kept open for forward compatibility
                        (`x-kubernetes-preserve-unknown-fields`).
                      properties:
                        input:
                          description: Input is a free-form example payload.
                          x-kubernetes-preserve-unknown-fields: true
                        note:
                          description: Note explains the example.
                          type: string
                        output:
                          description: Output is a free-form example payload.
                          x-kubernetes-preserve-unknown-fields: true
                      type: object
                    type: array
                  input:
                    description: SkillIO holds a JSON Schema describing input or output
                      of a Skill.
                    properties:
                      schema:
                        description: |-
                          Schema is the JSON Schema. Stored as opaque JSON so we can preserve
                          arbitrary OpenAPI features without taking a hard dep on Draft-7 vs
                          2020-12 semantics.
                        x-kubernetes-preserve-unknown-fields: true
                    required:
                    - schema
                    type: object
                  output:
                    description: SkillIO holds a JSON Schema describing input or output
                      of a Skill.
                    properties:
                      schema:
                        description: |-
                          Schema is the JSON Schema. Stored as opaque JSON so we can preserve
                          arbitrary OpenAPI features without taking a hard dep on Draft-7 vs
                          2020-12 semantics.
                        x-kubernetes-preserve-unknown-fields: true
                    required:
                    - schema
                    type: object
                required:
                - input
                - output
                type: object
              lifecycle:
                description: Lifecycle metadata.
                properties:
                  deprecation:
                    description: SkillDeprecation captures the lifecycle.deprecation
                      block.
                    properties:
                      migrationGuide:
                        description: MigrationGuide reference.
                        maxLength: 512
                        minLength: 8
                        pattern: ^(skill|agent|tool|model|data|prompt|channel|connector|memory|quota|ref|siem|policy)://[A-Za-z0-9._/\-]+(@[A-Za-z0-9._\-+]+)?$
                        type: string
                      successor:
                        description: Successor Skill ref users should migrate to.
                        maxLength: 512
                        minLength: 8
                        pattern: ^(skill|agent|tool|model|data|prompt|channel|connector|memory|quota|ref|siem|policy)://[A-Za-z0-9._/\-]+(@[A-Za-z0-9._\-+]+)?$
                        type: string
                      sunsetAt:
                        description: |-
                          SunsetAt is the cutoff time after which the Skill stops accepting
                          new references.
                        format: date-time
                        type: string
                    type: object
                type: object
              reliability:
                description: Reliability (timeout / retry / fallback / circuit-breaker).
                properties:
                  circuitBreaker:
                    description: CircuitBreaker policy.
                    properties:
                      enabled:
                        description: Enabled flips the breaker on/off without removing
                          config.
                        type: boolean
                      errorRateThreshold:
                        maximum: 1
                        minimum: 0
                        type: number
                      window:
                        description: |-
                          Duration is a coarse-grained, human-friendly duration string used in
                          CRDs. Supported units: ns, us, ms, s, m, h, d, w.

                          Validates: Requirements A2.2.
                        maxLength: 32
                        minLength: 2
                        pattern: ^\d+(ns|us|ms|s|m|h|d|w)$
                        type: string
                    type: object
                  fallback:
                    description: Fallback chain — multiple entries form an ordered
                      list.
                    items:
                      description: |-
                        FallbackSpec describes the fallback chain triggered when the primary
                        resource fails.
                      properties:
                        kind:
                          enum:
                          - Skill
                          - Agent
                          - StaticResponse
                          type: string
                        ref:
                          description: |-
                            ResourceRef is the canonical AIP URI used to reference any other resource
                            (skills, tools, models, data, prompts, channels, connectors, memory,
                            quota, ref, siem, policy). The accepted shape is
                            `<scheme>://<path>[@<version>]`.

                            Validates: Requirements A2.1 (regex enforcement), F25 (Parse∘Format
                            round-trip).
                          maxLength: 512
                          minLength: 8
                          pattern: ^(skill|agent|tool|model|data|prompt|channel|connector|memory|quota|ref|siem|policy)://[A-Za-z0-9._/\-]+(@[A-Za-z0-9._\-+]+)?$
                          type: string
                        staticResponse:
                          type: string
                      type: object
                    type: array
                  retries:
                    description: Retries policy.
                    properties:
                      backoff:
                        enum:
                        - fixed
                        - linear
                        - exponential
                        type: string
                      max:
                        description: Maximum number of retries.
                        format: int32
                        maximum: 10
                        minimum: 0
                        type: integer
                      retryOn:
                        description: RetryOn lists the failure classes that should
                          be retried.
                        items:
                          type: string
                        type: array
                        x-kubernetes-list-type: set
                    type: object
                  timeout:
                    description: Timeout for a single invocation.
                    maxLength: 32
                    minLength: 2
                    pattern: ^\d+(ns|us|ms|s|m|h|d|w)$
                    type: string
                type: object
              slo:
                description: SLO block.
                properties:
                  availability:
                    description: Availability as an SLO percentage string (e.g. "99.5%").
                    pattern: ^(100|[0-9]{1,2}(\.[0-9]+)?)%$
                    type: string
                  errorBudget:
                    description: ErrorBudget consumed by the resource.
                    maxLength: 32
                    minLength: 2
                    pattern: ^\d+(ns|us|ms|s|m|h|d|w)$
                    type: string
                  p95LatencyMs:
                    format: int32
                    minimum: 0
                    type: integer
                  p99LatencyMs:
                    format: int32
                    minimum: 0
                    type: integer
                  successRate:
                    description: SuccessRate as a fraction in [0, 1].
                    maximum: 1
                    minimum: 0
                    type: number
                type: object
              stability:
                description: Stability stage (experimental / beta / stable / deprecated).
                enum:
                - experimental
                - beta
                - stable
                - deprecated
                type: string
              version:
                description: Version is the skill version (strict semver).
                maxLength: 64
                minLength: 5
                pattern: ^(0|[1-9]\d*)\.(0|[1-9]\d*)\.(0|[1-9]\d*)(?:-((?:0|[1-9]\d*|\d*[a-zA-Z-][0-9a-zA-Z-]*)(?:\.(?:0|[1-9]\d*|\d*[a-zA-Z-][0-9a-zA-Z-]*))*))?(?:\+([0-9a-zA-Z-]+(?:\.[0-9a-zA-Z-]+)*))?$
                type: string
            required:
            - implementation
            - interface
            - stability
            - version
            type: object
          status:
            description: SkillStatus is the observed state of a Skill.
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              evalResults:
                description: SkillEvalResults is the most recent evaluation summary.
                properties:
                  lastRunAt:
                    format: date-time
                    type: string
                  metrics:
                    additionalProperties:
                      type: string
                    description: Metrics is metric → score.
                    type: object
                  passed:
                    description: Passed indicates whether gates were satisfied.
                    type: boolean
                type: object
              health:
                description: SkillHealth summarises runtime health metrics.
                properties:
                  costPerCallUsd:
                    description: |-
                      MoneyAmount is the canonical wire representation of a USD amount used
                      in CostBlock fields. We store money as a string with a constrained
                      pattern (rather than `float64`) to keep CRD diffs bit-for-bit
                      reproducible and to avoid the JSON-number rounding that breaks audit
                      hashing (Requirements F4, F22).
                    maxLength: 32
                    pattern: ^\d+(\.\d+)?$
                    type: string
                  last24hInvocations:
                    format: int64
                    type: integer
                  p95LatencyMs:
                    format: int32
                    type: integer
                  successRate:
                    type: number
                type: object
              observedGeneration:
                format: int64
                type: integer
              phase:
                description: |-
                  Phase is the coarse-grained status.phase value reported by every AIP
                  controller. The combined enum covers every controller in the platform
                  (Skill / Agent / Policy / Tool / DataSource / KB / ModelEndpoint /
                  ModelRouter / Tenant / SA / Budget / Quota), so a single Phase type can
                  be embedded in any status struct.

                  Validates: Requirements A3, A4, A5 (state machines reference these
                  values).
                enum:
                - Pending
                - Validating
                - Resolving
                - Building
                - Registering
                - Evaluating
                - Active
                - Degraded
                - Failed
                - Deprecated
                - Terminating
                - Suspended
                - Expired
                - ResolvingSkills
                - AttachingPolicies
                - Provisioning
                - Deploying
                - Configuring
                - RollingOut
                - Running
                - Paused
                - RolledBack
                type: string
              referencingAgents:
                description: |-
                  ReferencingAgents is the back-pointer used by the deletion finaliser
                  (Requirement A3.11).
                items:
                  type: string
                type: array
              resolvedDependencies:
                description: |-
                  SkillResolvedDependencies is what the controller wrote back after the
                  dependency resolver succeeded (Requirement A3.3).
                properties:
                  dataSources:
                    items:
                      description: |-
                        ResourceRef is the canonical AIP URI used to reference any other resource
                        (skills, tools, models, data, prompts, channels, connectors, memory,
                        quota, ref, siem, policy). The accepted shape is
                        `<scheme>://<path>[@<version>]`.

                        Validates: Requirements A2.1 (regex enforcement), F25 (Parse∘Format
                        round-trip).
                      maxLength: 512
                      minLength: 8
                      pattern: ^(skill|agent|tool|model|data|prompt|channel|connector|memory|quota|ref|siem|policy)://[A-Za-z0-9._/\-]+(@[A-Za-z0-9._\-+]+)?$
                      type: string
                    type: array
                  models:
                    items:
                      description: SkillResolvedModel records a resolved model dependency.
                      properties:
                        alias:
                          type: string
                        resolvedRef:
                          description: |-
                            ResourceRef is the canonical AIP URI used to reference any other resource
                            (skills, tools, models, data, prompts, channels, connectors, memory,
                            quota, ref, siem, policy). The accepted shape is
                            `<scheme>://<path>[@<version>]`.

                            Validates: Requirements A2.1 (regex enforcement), F25 (Parse∘Format
                            round-trip).
                          maxLength: 512
                          minLength: 8
                          pattern: ^(skill|agent|tool|model|data|prompt|channel|connector|memory|quota|ref|siem|policy)://[A-Za-z0-9._/\-]+(@[A-Za-z0-9._\-+]+)?$
                          type: string
                      required:
                      - alias
                      - resolvedRef
                      type: object
                    type: array
                  skills:
                    items:
                      description: |-
                        ResourceRef is the canonical AIP URI used to reference any other resource
                        (skills, tools, models, data, prompts, channels, connectors, memory,
                        quota, ref, siem, policy). The accepted shape is
                        `<scheme>://<path>[@<version>]`.

                        Validates: Requirements A2.1 (regex enforcement), F25 (Parse∘Format
                        round-trip).
                      maxLength: 512
                      minLength: 8
                      pattern: ^(skill|agent|tool|model|data|prompt|channel|connector|memory|quota|ref|siem|policy)://[A-Za-z0-9._/\-]+(@[A-Za-z0-9._\-+]+)?$
                      type: string
                    type: array
                  tools:
                    items:
                      description: |-
                        ResourceRef is the canonical AIP URI used to reference any other resource
                        (skills, tools, models, data, prompts, channels, connectors, memory,
                        quota, ref, siem, policy). The accepted shape is
                        `<scheme>://<path>[@<version>]`.

                        Validates: Requirements A2.1 (regex enforcement), F25 (Parse∘Format
                        round-trip).
                      maxLength: 512
                      minLength: 8
                      pattern: ^(skill|agent|tool|model|data|prompt|channel|connector|memory|quota|ref|siem|policy)://[A-Za-z0-9._/\-]+(@[A-Za-z0-9._\-+]+)?$
                      type: string
                    type: array
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
  - additionalPrinterColumns:
    - jsonPath: .spec.version
      name: Version
      type: string
    - jsonPath: .spec.stability
      name: Stage
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: Skill is the platform's unit of reusable business capability
          (v1beta1).
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SkillSpec is the desired state of a Skill (v1beta1).
            properties:
              compliance:
                description: Compliance block — new in v1beta1.
                properties:
                  auditRequired:
                    description: AuditRequired forces forensic-level auditing when
                      true.
                    type: boolean
                  dataClassification:
                    description: DataClassification indicates the highest data sensitivity
                      level.
                    enum:
                    - public
                    - internal
                    - confidential
                    - restricted
                    type: string
                  standards:
                    description: Standards lists the compliance frameworks this Skill
                      satisfies.
                    items:
                      description: SkillComplianceStandard references a specific compliance
                        standard.
                      properties:
                        controls:
                          description: Controls lists the specific control IDs satisfied.
                          items:
                            type: string
                          type: array
                        name:
                          description: Name of the compliance standard (e.g. "SOC2",
                            "GDPR", "HIPAA", "ISO27001").
                          maxLength: 64
                          minLength: 1
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                type: object
              cost:
                description: Cost block.
                properties:
                  budget:
                    description: Budget per call.
                    properties:
                      tokensPerCall:
                        format: int64
                        minimum: 0
                        type: integer
                      usdPerCall:
                        description: |-
                          MoneyAmount is the canonical wire representation of a USD amount used
                          in CostBlock fields. We store money as a string with a constrained
                          pattern (rather than `float64`) to keep CRD diffs bit-for-bit
                          reproducible and to avoid the JSON-number rounding that breaks audit
                          hashing (Requirements F4, F22).
                        maxLength: 32
                        pattern: ^\d+(\.\d+)?$
                        type: string
                    type: object
                  estimator:
                    description: Estimator config.
                    properties:
                      historicalWindow:
                        description: HistoricalWindow used by historical estimators.
                        maxLength: 32
                        minLength: 2
                        pattern: ^\d+(ns|us|ms|s|m|h|d|w)$
                        type: string
                      tokensPerCall:
                        description: TokensPerCall percentiles.
                        properties:
                          p50:
                            description: |-
                              MoneyAmount is the canonical wire representation of a USD amount used
                              in CostBlock fields. We store money as a string with a constrained
                              pattern (rather than `float64`) to keep CRD diffs bit-for-bit
                              reproducible and to avoid the JSON-number rounding that breaks audit
                              hashing (Requirements F4, F22).
                            maxLength: 32
                            pattern: ^\d+(\.\d+)?$
                            type: string
                          p95:
                            description: |-
                              MoneyAmount is the canonical wire representation of a USD amount used
                              in CostBlock fields. We store money as a string with a constrained
                              pattern (rather than `float64`) to keep CRD diffs bit-for-bit
                              reproducible and to avoid the JSON-number rounding that breaks audit
                              hashing (Requirements F4, F22).
                            maxLength: 32
                            pattern: ^\d+(\.\d+)?$
                            type: string
                          p99:
                            description: |-
                              MoneyAmount is the canonical wire representation of a USD amount used
                              in CostBlock fields. We store money as a string with a constrained
                              pattern (rather than `float64`) to keep CRD diffs bit-for-bit
                              reproducible and to avoid the JSON-number rounding that breaks audit
                              hashing (Requirements F4, F22).
                            maxLength: 32
                            pattern: ^\d+(\.\d+)?$
                            type: string
                        type: object
                      type:
                        description: Type of estimator.
                        enum:
                        - static
                        - model_based
                        - historical
                        type: string
                      usdPerCall:
                        description: UsdPerCall percentiles.
                        properties:
                          p50:
                            description: |-
                              MoneyAmount is the canonical wire representation of a USD amount used
                              in CostBlock fields. We store money as a string with a constrained
                              pattern (rather than `float64`) to keep CRD diffs bit-for-bit
                              reproducible and to avoid the JSON-number rounding that breaks audit
                              hashing (Requirements F4, F22).
                            maxLength: 32
                            pattern: ^\d+(\.\d+)?$
                            type: string
                          p95:
                            description: |-
                              MoneyAmount is the canonical wire representation of a USD amount used
                              in CostBlock fields. We store money as a string with a constrained
                              pattern (rather than `float64`) to keep CRD diffs bit-for-bit
                              reproducible and to avoid the JSON-number rounding that breaks audit
                              hashing (Requirements F4, F22).
                            maxLength: 32
                            pattern: ^\d+(\.\d+)?$
                            type: string
                          p99:
                            description: |-
                              MoneyAmount is the canonical wire representation of a USD amount used
                              in CostBlock fields. We store money as a string with a constrained
                              pattern (rather than `float64`) to keep CRD diffs bit-for-bit
                              reproducible and to avoid the JSON-number rounding that breaks audit
                              hashing (Requirements F4, F22).
                            maxLength: 32
                            pattern: ^\d+(\.\d+)?$
                            type: string
                        type: object
                    type: object
                type: object
              evaluation:
                description: Evaluation suite for stage gating (enhanced in v1beta1).
                properties:
                  continuousEval:
                    description: |-
                      ContinuousEval enables always-on evaluation in production.
                      New in v1beta1.
                    type: boolean
                  evalSet:
                    description: |-
                      ResourceRef is the canonical AIP URI used to reference any other resource
                      (skills, tools, models, data, prompts, channels, connectors, memory,
                      quota, ref, siem, policy). The accepted shape is
                      `<scheme>://<path>[@<version>]`.

                      Validates: Requirements A2.1 (regex enforcement), F25 (Parse∘Format
                      round-trip).
                    maxLength: 512
                    minLength: 8
                    pattern: ^(skill|agent|tool|model|data|prompt|channel|connector|memory|quota|ref|siem|policy)://[A-Za-z0-9._/\-]+(@[A-Za-z0-9._\-+]+)?$
                    type: string
                  gates:
                    additionalProperties:
                      additionalProperties:
                        type: string
                      type: object
                    description: |-
                      Gates for stage promotion. Outer key = target stage, inner map =
                      metric → CEL expression.
                    type: object
                  metrics:
                    description: |-
                      Metrics provides structured metric definitions with thresholds.
                      New in v1beta1.
                    items:
                      description: |-
                        SkillEvalMetric defines a single eval metric with thresholds.
                        New in v1beta1 — provides structured metric definitions.
                      properties:
                        name:
                          description: Name of the metric (e.g. "accuracy", "latency_p95",
                            "toxicity").
                          maxLength: 64
                          minLength: 1
                          type: string
                        threshold:
                          description: Threshold is a CEL expression evaluating whether
                            the metric passes.
                          maxLength: 512
                          type: string
                        weight:
                          description: Weight for composite scoring (0.0–1.0).
                          maximum: 1
                          minimum: 0
                          type: number
                      required:
                      - name
                      type: object
                    type: array
                  redTeamSet:
                    description: |-
                      ResourceRef is the canonical AIP URI used to reference any other resource
                      (skills, tools, models, data, prompts, channels, connectors, memory,
                      quota, ref, siem, policy). The accepted shape is
                      `<scheme>://<path>[@<version>]`.

                      Validates: Requirements A2.1 (regex enforcement), F25 (Parse∘Format
                      round-trip).
                    maxLength: 512
                    minLength: 8
                    pattern: ^(skill|agent|tool|model|data|prompt|channel|connector|memory|quota|ref|siem|policy)://[A-Za-z0-9._/\-]+(@[A-Za-z0-9._\-+]+)?$
                    type: string
                  schedule:
                    maxLength: 128
                    type: string
                type: object
              governance:
                description: Governance block.
                properties:
                  classification:
                    description: Classification applied to data flowing through the
                      resource.
                    enum:
                    - public
                    - internal
                    - confidential
                    - restricted
                    - secret
                    type: string
                  compliance:
                    description: Compliance regimes required for this resource.
                    properties:
                      reportTemplate:
                        description: ReportTemplate references a compliance reporting
                          template.
                        maxLength: 512
                        minLength: 8
                        pattern: ^(skill|agent|tool|model|data|prompt|channel|connector|memory|quota|ref|siem|policy)://[A-Za-z0-9._/\-]+(@[A-Za-z0-9._\-+]+)?$
                        type: string
                      required:
                        description: Required compliance tags (e.g. "GDPR", "HIPAA",
                          "等保三级").
                        items:
                          type: string
                        type: array
                    type: object
                  dataResidency:
                    description: DataResidency controls geographic placement of processing.
                    properties:
                      allowedRegions:
                        description: |-
                          AllowedRegions lists the regions in which a request may be
                          processed (e.g. "cn-north", "eu-west").
                        items:
                          type: string
                        type: array
                      crossBorder:
                        description: |-
                          CrossBorder controls whether transfers across the residency
                          boundary are permitted.
                        enum:
                        - forbidden
                        - allowed_with_approval
                        - allowed
                        type: string
                    type: object
                  holds:
                    description: |-
                      Holds carries legal-hold tags. Audit events inheriting these tags
                      are exempt from automatic deletion until the holds are cleared.
                    items:
                      type: string
                    type: array
                  pii:
                    description: PII policy.
                    properties:
                      onInput:
                        description: OnInput controls inbound DLP behaviour.
                        enum:
                        - ignore
                        - detect
                        - detect_and_mask
                        - detect_and_block
                        type: string
                      onOutput:
                        description: OnOutput controls outbound DLP behaviour.
                        enum:
                        - ignore
                        - detect
                        - detect_and_mask
                        - detect_and_block
                        type: string
                      patternsRef:
                        description: |-
                          PatternsRef points at a `ref://patterns/<name>` resource that holds
                          the regex / dictionary patterns the DLP engine uses.
                        maxLength: 512
                        minLength: 8
                        pattern: ^(skill|agent|tool|model|data|prompt|channel|connector|memory|quota|ref|siem|policy)://[A-Za-z0-9._/\-]+(@[A-Za-z0-9._\-+]+)?$
                        type: string
                    type: object
                  retentionDays:
                    description: |-
                      RetentionDays is how many days raw payloads may be retained
                      (audit-related; mirrors `retention` Duration on Skill governance
                      but is expressed in days for budget UIs).
                    format: int32
                    maximum: 36500
                    minimum: 0
                    type: integer
                type: object
              implementation:
                description: Implementation runtime.
                properties:
                  promptTemplate:
                    description: SkillPromptTemplate references or inlines a prompt
                      template.
                    properties:
                      inline:
                        maxLength: 65536
                        type: string
                      ref:
                        description: |-
                          ResourceRef is the canonical AIP URI used to reference any other resource
                          (skills, tools, models, data, prompts, channels, connectors, memory,
                          quota, ref, siem, policy). The accepted shape is
                          `<scheme>://<path>[@<version>]`.

                          Validates: Requirements A2.1 (regex enforcement), F25 (Parse∘Format
                          round-trip).
                        maxLength: 512
                        minLength: 8
                        pattern: ^(skill|agent|tool|model|data|prompt|channel|connector|memory|quota|ref|siem|policy)://[A-Za-z0-9._/\-]+(@[A-Za-z0-9._\-+]+)?$
                        type: string
                    type: object
                  requires:
                    description: SkillRequires lists external dependencies of a Skill.
                    properties:
                      dataSources:
                        items:
                          description: SkillDataSourceDep depends on a DataSource
                            (or KnowledgeBase).
                          properties:
                            ref:
                              description: |-
                                ResourceRef is the canonical AIP URI used to reference any other resource
                                (skills, tools, models, data, prompts, channels, connectors, memory,
                                quota, ref, siem, policy). The accepted shape is
                                `<scheme>://<path>[@<version>]`.

                                Validates: Requirements A2.1 (regex enforcement), F25 (Parse∘Format
                                round-trip).
                              maxLength: 512
                              minLength: 8
                              pattern: ^(skill|agent|tool|model|data|prompt|channel|connector|memory|quota|ref|siem|policy)://[A-Za-z0-9._/\-]+(@[A-Za-z0-9._\-+]+)?$
                              type: string
                          required:
                          - ref
                          type: object
                        type: array
                      models:
                        items:
                          description: SkillModelDep is a dependency on a model alias.
                          properties:
                            alias:
                              maxLength: 64
                              minLength: 1
                              type: string
                            fallback:
                              items:
                                description: |-
                                  ResourceRef is the canonical AIP URI used to reference any other resource
                                  (skills, tools, models, data, prompts, channels, connectors, memory,
                                  quota, ref, siem, policy). The accepted shape is
                                  `<scheme>://<path>[@<version>]`.

                                  Validates: Requirements A2.1 (regex enforcement), F25 (Parse∘Format
                                  round-trip).
                                maxLength: 512
                                minLength: 8
                                pattern: ^(skill|agent|tool|model|data|prompt|channel|connector|memory|quota|ref|siem|policy)://[A-Za-z0-9._/\-]+(@[A-Za-z0-9._\-+]+)?$
                                type: string
                              type: array
                            purpose:
                              enum:
                              - reasoning
                              - embedding
                              - vision
                              - code
                              - classification
                              - rerank
                              type: string
                            ref:
                              description: |-
                                ResourceRef is the canonical AIP URI used to reference any other resource
                                (skills, tools, models, data, prompts, channels, connectors, memory,
                                quota, ref, siem, policy). The accepted shape is
                                `<scheme>://<path>[@<version>]`.

                                Validates: Requirements A2.1 (regex enforcement), F25 (Parse∘Format
                                round-trip).
                              maxLength: 512
                              minLength: 8
                              pattern: ^(skill|agent|tool|model|data|prompt|channel|connector|memory|quota|ref|siem|policy)://[A-Za-z0-9._/\-]+(@[A-Za-z0-9._\-+]+)?$
                              type: string
                          required:
                          - alias
                          - ref
                          type: object
                        type: array
                      skills:
                        items:
                          description: SkillSubSkillDep depends on another Skill.
                          properties:
                            ref:
                              description: |-
                                ResourceRef is the canonical AIP URI used to reference any other resource
                                (skills, tools, models, data, prompts, channels, connectors, memory,
                                quota, ref, siem, policy). The accepted shape is
                                `<scheme>://<path>[@<version>]`.

                                Validates: Requirements A2.1 (regex enforcement), F25 (Parse∘Format
                                round-trip).
                              maxLength: 512
                              minLength: 8
                              pattern: ^(skill|agent|tool|model|data|prompt|channel|connector|memory|quota|ref|siem|policy)://[A-Za-z0-9._/\-]+(@[A-Za-z0-9._\-+]+)?$
                              type: string
                            versionConstraint:
                              maxLength: 128
                              type: string
                          required:
                          - ref
                          type: object
                        type: array
                      tools:
                        items:
                          description: SkillToolDep depends on a Tool resource.
                          properties:
                            ref:
                              description: |-
                                ResourceRef is the canonical AIP URI used to reference any other resource
                                (skills, tools, models, data, prompts, channels, connectors, memory,
                                quota, ref, siem, policy). The accepted shape is
                                `<scheme>://<path>[@<version>]`.

                                Validates: Requirements A2.1 (regex enforcement), F25 (Parse∘Format
                                round-trip).
                              maxLength: 512
                              minLength: 8
                              pattern: ^(skill|agent|tool|model|data|prompt|channel|connector|memory|quota|ref|siem|policy)://[A-Za-z0-9._/\-]+(@[A-Za-z0-9._\-+]+)?$
                              type: string
                          required:
                          - ref
                          type: object
                        type: array
                    type: object
                  runtime:
                    description: SkillRuntime describes how the implementation is
                      deployed.
                    properties:
                      engine:
                        type: string
                      entrypoint:
                        type: string
                      image:
                        type: string
                    type: object
                  type:
                    enum:
                    - function
                    - workflow
                    - agentic
                    - mcp_tool
                    - external_api
                    type: string
                required:
                - type
                type: object
              interface:
                description: Interface contract.
                properties:
                  examples:
                    items:
                      description: SkillExample is an input/output example pair attached
                        to the Skill interface.
                      properties:
                        input:
                          x-kubernetes-preserve-unknown-fields: true
                        note:
                          type: string
                        output:
                          x-kubernetes-preserve-unknown-fields: true
                      type: object
                    type: array
                  input:
                    description: SkillIO holds a JSON Schema describing input or output
                      of a Skill.
                    properties:
                      schema:
                        x-kubernetes-preserve-unknown-fields: true
                    required:
                    - schema
                    type: object
                  output:
                    description: SkillIO holds a JSON Schema describing input or output
                      of a Skill.
                    properties:
                      schema:
                        x-kubernetes-preserve-unknown-fields: true
                    required:
                    - schema
                    type: object
                required:
                - input
                - output
                type: object
              lifecycle:
                description: Lifecycle metadata.
                properties:
                  deprecation:
                    description: SkillDeprecation captures the lifecycle.deprecation
                      block.
                    properties:
                      migrationGuide:
                        description: |-
                          ResourceRef is the canonical AIP URI used to reference any other resource
                          (skills, tools, models, data, prompts, channels, connectors, memory,
                          quota, ref, siem, policy). The accepted shape is
                          `<scheme>://<path>[@<version>]`.

                          Validates: Requirements A2.1 (regex enforcement), F25 (Parse∘Format
                          round-trip).
                        maxLength: 512
                        minLength: 8
                        pattern: ^(skill|agent|tool|model|data|prompt|channel|connector|memory|quota|ref|siem|policy)://[A-Za-z0-9._/\-]+(@[A-Za-z0-9._\-+]+)?$
                        type: string
                      successor:
                        description: |-
                          ResourceRef is the canonical AIP URI used to reference any other resource
                          (skills, tools, models, data, prompts, channels, connectors, memory,
                          quota, ref, siem, policy). The accepted shape is
                          `<scheme>://<path>[@<version>]`.

                          Validates: Requirements A2.1 (regex enforcement), F25 (Parse∘Format
                          round-trip).
                        maxLength: 512
                        minLength: 8
                        pattern: ^(skill|agent|tool|model|data|prompt|channel|connector|memory|quota|ref|siem|policy)://[A-Za-z0-9._/\-]+(@[A-Za-z0-9._\-+]+)?$
                        type: string
                      sunsetAt:
                        format: date-time
                        type: string
                    type: object
                type: object
              reliability:
                description: Reliability (timeout / retry / fallback / circuit-breaker).
                properties:
                  circuitBreaker:
                    description: CircuitBreaker policy.
                    properties:
                      enabled:
                        description: Enabled flips the breaker on/off without removing
                          config.
                        type: boolean
                      errorRateThreshold:
                        maximum: 1
                        minimum: 0
                        type: number
                      window:
                        description: |-
                          Duration is a coarse-grained, human-friendly duration string used in
                          CRDs. Supported units: ns, us, ms, s, m, h, d, w.

                          Validates: Requirements A2.2.
                        maxLength: 32
                        minLength: 2
                        pattern: ^\d+(ns|us|ms|s|m|h|d|w)$
                        type: string
                    type: object
                  fallback:
                    description: Fallback chain — multiple entries form an ordered
                      list.
                    items:
                      description: |-
                        FallbackSpec describes the fallback chain triggered when the primary
                        resource fails.
                      properties:
                        kind:
                          enum:
                          - Skill
                          - Agent
                          - StaticResponse
                          type: string
                        ref:
                          description: |-
                            ResourceRef is the canonical AIP URI used to reference any other resource
                            (skills, tools, models, data, prompts, channels, connectors, memory,
                            quota, ref, siem, policy). The accepted shape is
                            `<scheme>://<path>[@<version>]`.

                            Validates: Requirements A2.1 (regex enforcement), F25 (Parse∘Format
                            round-trip).
                          maxLength: 512
                          minLength: 8
                          pattern: ^(skill|agent|tool|model|data|prompt|channel|connector|memory|quota|ref|siem|policy)://[A-Za-z0-9._/\-]+(@[A-Za-z0-9._\-+]+)?$
                          type: string
                        staticResponse:
                          type: string
                      type: object
                    type: array
                  retries:
                    description: Retries policy.
                    properties:
                      backoff:
                        enum:
                        - fixed
                        - linear
                        - exponential
                        type: string
                      max:
                        description: Maximum number of retries.
                        format: int32
                        maximum: 10
                        minimum: 0
                        type: integer
                      retryOn:
                        description: RetryOn lists the failure classes that should
                          be retried.
                        items:
                          type: string
                        type: array
                        x-kubernetes-list-type: set
                    type: object
                  timeout:
                    description: Timeout for a single invocation.
                    maxLength: 32
                    minLength: 2
                    pattern: ^\d+(ns|us|ms|s|m|h|d|w)$
                    type: string
                type: object
              slo:
                description: SLO block.
                properties:
                  availability:
                    description: Availability as an SLO percentage string (e.g. "99.5%").
                    pattern: ^(100|[0-9]{1,2}(\.[0-9]+)?)%$
                    type: string
                  errorBudget:
                    description: ErrorBudget consumed by the resource.
                    maxLength: 32
                    minLength: 2
                    pattern: ^\d+(ns|us|ms|s|m|h|d|w)$
                    type: string
                  p95LatencyMs:
                    format: int32
                    minimum: 0
                    type: integer
                  p99LatencyMs:
                    format: int32
                    minimum: 0
                    type: integer
                  successRate:
                    description: SuccessRate as a fraction in [0, 1].
                    maximum: 1
                    minimum: 0
                    type: number
                type: object
              stability:
                description: Stability stage (experimental / beta / stable / deprecated).
                enum:
                - experimental
                - beta
                - stable
                - deprecated
                type: string
              version:
                description: Version is the skill version (strict semver).
                maxLength: 64
                minLength: 5
                pattern: ^(0|[1-9]\d*)\.(0|[1-9]\d*)\.(0|[1-9]\d*)(?:-((?:0|[1-9]\d*|\d*[a-zA-Z-][0-9a-zA-Z-]*)(?:\.(?:0|[1-9]\d*|\d*[a-zA-Z-][0-9a-zA-Z-]*))*))?(?:\+([0-9a-zA-Z-]+(?:\.[0-9a-zA-Z-]+)*))?$
                type: string
            required:
            - implementation
            - interface
            - stability
            - version
            type: object
          status:
            description: SkillStatus is the observed state of a Skill.
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              evalResults:
                description: SkillEvalResults is the most recent evaluation summary.
                properties:
                  lastRunAt:
                    format: date-time
                    type: string
                  metrics:
                    additionalProperties:
                      type: string
                    type: object
                  passed:
                    type: boolean
                type: object
              health:
                description: SkillHealth summarises runtime health metrics.
                properties:
                  costPerCallUsd:
                    description: |-
                      MoneyAmount is the canonical wire representation of a USD amount used
                      in CostBlock fields. We store money as a string with a constrained
                      pattern (rather than `float64`) to keep CRD diffs bit-for-bit
                      reproducible and to avoid the JSON-number rounding that breaks audit
                      hashing (Requirements F4, F22).
                    maxLength: 32
                    pattern: ^\d+(\.\d+)?$
                    type: string
                  last24hInvocations:
                    format: int64
                    type: integer
                  p95LatencyMs:
                    format: int32
                    type: integer
                  successRate:
                    type: number
                type: object
              observedGeneration:
                format: int64
                type: integer
              phase:
                description: |-
                  Phase is the coarse-grained status.phase value reported by every AIP
                  controller. The combined enum covers every controller in the platform
                  (Skill / Agent / Policy / Tool / DataSource / KB / ModelEndpoint /
                  ModelRouter / Tenant / SA / Budget / Quota), so a single Phase type can
                  be embedded in any status struct.

                  Validates: Requirements A3, A4, A5 (state machines reference these
                  values).
                enum:
                - Pending
                - Validating
                - Resolving
                - Building
                - Registering
                - Evaluating
                - Active
                - Degraded
                - Failed
                - Deprecated
                - Terminating
                - Suspended
                - Expired
                - ResolvingSkills
                - AttachingPolicies
                - Provisioning
                - Deploying
                - Configuring
                - RollingOut
                - Running
                - Paused
                - RolledBack
                type: string
              referencingAgents:
                items:
                  type: string
                type: array
              resolvedDependencies:
                description: |-
                  SkillResolvedDependencies is what the controller wrote back after the
                  dependency resolver succeeded.
                properties:
                  dataSources:
                    items:
                      description: |-
                        ResourceRef is the canonical AIP URI used to reference any other resource
                        (skills, tools, models, data, prompts, channels, connectors, memory,
                        quota, ref, siem, policy). The accepted shape is
                        `<scheme>://<path>[@<version>]`.

                        Validates: Requirements A2.1 (regex enforcement), F25 (Parse∘Format
                        round-trip).
                      maxLength: 512
                      minLength: 8
                      pattern: ^(skill|agent|tool|model|data|prompt|channel|connector|memory|quota|ref|siem|policy)://[A-Za-z0-9._/\-]+(@[A-Za-z0-9._\-+]+)?$
                      type: string
                    type: array
                  models:
                    items:
                      description: SkillResolvedModel records a resolved model dependency.
                      properties:
                        alias:
                          type: string
                        resolvedRef:
                          description: |-
                            ResourceRef is the canonical AIP URI used to reference any other resource
                            (skills, tools, models, data, prompts, channels, connectors, memory,
                            quota, ref, siem, policy). The accepted shape is
                            `<scheme>://<path>[@<version>]`.

                            Validates: Requirements A2.1 (regex enforcement), F25 (Parse∘Format
                            round-trip).
                          maxLength: 512
                          minLength: 8
                          pattern: ^(skill|agent|tool|model|data|prompt|channel|connector|memory|quota|ref|siem|policy)://[A-Za-z0-9._/\-]+(@[A-Za-z0-9._\-+]+)?$
                          type: string
                      required:
                      - alias
                      - resolvedRef
                      type: object
                    type: array
                  skills:
                    items:
                      description: |-
                        ResourceRef is the canonical AIP URI used to reference any other resource
                        (skills, tools, models, data, prompts, channels, connectors, memory,
                        quota, ref, siem, policy). The accepted shape is
                        `<scheme>://<path>[@<version>]`.

                        Validates: Requirements A2.1 (regex enforcement), F25 (Parse∘Format
                        round-trip).
                      maxLength: 512
                      minLength: 8
                      pattern: ^(skill|agent|tool|model|data|prompt|channel|connector|memory|quota|ref|siem|policy)://[A-Za-z0-9._/\-]+(@[A-Za-z0-9._\-+]+)?$
                      type: string
                    type: array
                  tools:
                    items:
                      description: |-
                        ResourceRef is the canonical AIP URI used to reference any other resource
                        (skills, tools, models, data, prompts, channels, connectors, memory,
                        quota, ref, siem, policy). The accepted shape is
                        `<scheme>://<path>[@<version>]`.

                        Validates: Requirements A2.1 (regex enforcement), F25 (Parse∘Format
                        round-trip).
                      maxLength: 512
                      minLength: 8
                      pattern: ^(skill|agent|tool|model|data|prompt|channel|connector|memory|quota|ref|siem|policy)://[A-Za-z0-9._/\-]+(@[A-Za-z0-9._\-+]+)?$
                      type: string
                    type: array
                type: object
            type: object
        type: object
    served: true
    storage: false
    subresources:
      status: {}
